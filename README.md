# Pure Python Server

A simple, dependency-free HTTP server built with Python's standard library only.

## How to run

```bash
python server.py
```

Open http://localhost:8000 in your browser.

Serve any static files by placing them in the same directory.

## To make it more advanced

You can extend `SimpleHTTPRequestHandler` for custom routing, POST handling, etc.

No external dependencies. Pure Python.